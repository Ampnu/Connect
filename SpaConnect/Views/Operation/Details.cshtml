@model IEnumerable<SpaConnect.Models.Operation>

@{
    ViewBag.Title = "OPERATIONS";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>OPERATIONS</h2>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>OPN</th>
            <th>OPERATION</th>
            <th>MODIFY</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var val in Model)
        {
            <tr>
                <td>@val.OPN</td>
                <td>@Html.ActionLink(val.opTitle, "Index", "Step", new { id = val.ID }, null)</td>
                <td>
                    @Html.ActionLink("New", "New", "Operation", new { id = val.ID }, new { @class = "btn btn-primary btn-xs" })
                    @Html.ActionLink("Edit", "Edit1", "Operation", new { id = val.ID }, new { @class = "btn btn-primary btn-xs" })
                    @Html.ActionLink("Delete", "Result", "Operation", new { id = val.ID }, new { @class = "btn btn-primary btn-xs" })
                </td>
            </tr>
        }
    </tbody>
</table>




